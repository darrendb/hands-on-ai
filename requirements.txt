# we are using:
# - pyenv
# - python 3.11.11
# - seems that installing one at a time via pip cli works better than using pip install -r requirements.txt

# Core dependencies
python-dotenv==1.1.0
pdfplumber==0.10.3

# LangChain and related dependencies
openai==1.2.3
langchain==0.0.334
# langchain giving us this error: ```Client.init() got an unexpected keyword argument 'proxies' (type=type_error)```
# setting httpx to 0.27.2 fixes this issue
httpx==0.27.2
chromadb==0.4.17
tiktoken==0.5.1

# Chainlit for UI
chainlit==0.7.501

# can't get pysqlite3-binary==0.5.2.post1 to install with pip
# pysqlite3-binary==0.5.2.post1
# ./pysqlite3_binary-0.5.2.post1-cp311-cp311-manylinux_2_17_x86_64.manylinux2014_x86_64.whl
# even fails using local wheel
# pysqlite3_binary-0.5.2.post1-cp311-cp311-manylinux_2_17_x86_64.manylinux2014_x86_64.whl